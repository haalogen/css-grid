{
  "name": "css-grid",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --no-open --no-inject-changes --server --files '**/*.css, **/*.html, **/*.js' --directory --port 7777"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.23.6"
  }
}
